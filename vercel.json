{
    "devCommand": "uvicorn skindentity.main:app --host 0.0.0.0 --port 3000",
    "builds": [
        {
        "src": "skindentity/main.py",
        "use": "@vercel/python"
        }
    ],
    "routes": [
        {
        "src": "/(.*)",
        "dest": "skindentity/main.py"
        }
    ]
}